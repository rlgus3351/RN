import { TouchableOpacity } from "react-native";
import {Ionicons} from '@expo/vector-icons';
export default () =>{
    return(
        <TouchableOpacity>
            <Ionicons name="star" size={24} color={}/>
        </TouchableOpacity>
    )
}